// import { Router } from 'express';
// import { createProxyMiddleware } from 'http-proxy-middleware';
// import { services } from '../config/config';

// const router = Router();

// router.use('/api/auth', createProxyMiddleware({ target: services.auth, changeOrigin: true }));
// router.use('/api/products', createProxyMiddleware({ target: services.product, changeOrigin: true }));
// router.use('/api/users', createProxyMiddleware({ target: services.user, changeOrigin: true }));
// router.use('/api/cart', createProxyMiddleware({ target: services.cart, changeOrigin: true }));
// router.use('/api/order', createProxyMiddleware({ target: services.order, changeOrigin: true }));
// router.use('/api/inventory', createProxyMiddleware({ target: services.inventory, changeOrigin: true }));

// export default router;
